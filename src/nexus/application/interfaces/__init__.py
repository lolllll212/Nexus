"""
Application service interfaces - facades the infrastructure consumes.

These coordinate use cases. Infrastructure (API, workers) depends on these,
never reaching directly into individual use cases it doesn't own.
"""
